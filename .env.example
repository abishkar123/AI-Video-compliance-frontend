# Frontend env (Vite exposes only VITE_ prefixed vars to the browser)
# The proxy in vite.config.js forwards /api and /health to the backend,
# so no API URL is needed in the browser bundle.
VITE_APP_NAME=ComplianceQA
